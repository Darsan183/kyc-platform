apiVersion: v1
kind: Secret
metadata:
  name: kyc-secrets
  namespace: kyc-platform
type: Opaque
stringData:
  database-url: jdbc:postgresql://kyc-postgres:5432/kycplatform
  jwt-secret: ${JWT_SECRET}
  postgres-password: ${POSTGRES_PASSWORD}